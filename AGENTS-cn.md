# AGENTS.md

## 绝对原则

1. 无一例外地**执行** task/skill文件中的所有规则和要求。
2. **完成** 每个task的所有进入和退出条件。
3. 在关卡处 **停止** —— 仅在条件满足时继续。

## 初始设置 [仅限首次]

**在执行任何其他操作之前完成：**
1. 执行 `date` 命令 → 存储为 `SESSION_BASELINE_DATE`(date结果的格式举例：2026-05-31)。
2. 应用 `.agents/skills/metacognition/SKILL.md` → 在整个会话期间保持激活。
3. 为所有日期引用（Web搜索、文档等）使用 `SESSION_BASELINE_DATE`。
4. 使用 `ls -la` 验证项目结构。

## 核心执行原则

**通用入口点**：每个诉求都从 `task-analysis.md` 开始，以确定合适的路径。

## 任务分析 - 必要的第一步

**提前退出检查（在加载 `task-analysis.md` 之前）：**
此任务是否需要读取代码文件来决定 **如何** 执行它？
- **否** → 直接执行，跳过 `task-analysis.md`。
- **是** → 加载并遵循 `task-analysis.md`。

**如果不是提前退出，任何用户诉求都从这里开始：**
1. 应用 `.agents/tasks/task-analysis.md`。
2. 遵循其输出选择合适的路径：

### 基于任务分析的路径选择

**小规模（1-2个文件）/ 单一任务：**
- 加载特定的任务定义（例如，`implementation.md`, `technical-design.md`）。
- 直接执行该任务定义。
- 不需要工作流。

**中/大规模（3个以上文件）/ 复杂任务：**
- 遵循 `task-analysis.md` 关于工作流选择的建议。

## 核心原则

### 计划注入 [强制执行]
**所有任务都需要计划注入以进行阻塞性读取（BLOCKING READs）：**
- `task-analysis.md` 的第8步扫描并识别所有 **阻塞性读取** 需求。
- 工作计划 **必须** 包含来自工作流/任务/skills的每一个 **阻塞性读取**。
- 每个阶段验证其 **阻塞性读取** 是否在计划中。
- 关卡在继续之前验证计划注入的证据。
- 缺少 **任何** 阻塞性读取 = **立即暂停**。

### 任务定义加载
**任务定义了要构建的“内容” —— 绝不能跳过它们：**
- 在继续之前验证入口关卡。
- 遵循每个任务定义中的“所需skills”部分。

### skills应用
**根据 `task-analysis` 的任务类型应用skills：**
- skills是根据需要逐步加载的。
- 每个任务定义都指定了其所需的skills。
- 完成后卸载特定于任务的skills。

### 质量标准
**在将任何任务标记为完成之前：**
- 所有测试通过（如适用）。
- 所有质量检查返回0个错误。
- 任务退出条件得到满足。
- 按需记录工作。

## 批准点(Approval Points)

**原则**：在重要的里程碑处获得用户批准。

常见的批准点：
- 为中/大型任务推荐工作流时。
- 创建设计或决策文档后。
- 当技术方案发生重大变化时。
- 在任务定义指定的停止点。

**需防止的违规行为：**
- 工作计划中未包含 **所有** 阻塞性读取 = **返回任务分析**。
- 跳过 **任何** 阻塞性读取 = **立即暂停**。
- 未遵守任务定义继续进行 = **阻塞性错误**。

## 质量标准

**通用质量要求：**
- 所有代码变更遵循TDD（测试驱动开发）流程。
- 所有质量检查必须以0错误通过。
- 遵循特定语言skills中定义的标准。
- 每个任务定义都指定了其质量关卡。

## 元认知检查点

在这些强制性节点执行自我评估：
- 任务类型发生变化时。
- 出现意外错误时。
- 完成一个有意义的工作单元时。
- 开始新的实现之前。
- 完成工作计划中的每个任务之后。

## 上下文管理

**指南**：
-   逐步加载skills，而不是一次性全部加载。
-   完成后卸载特定于任务的skills。
-   只保留频繁使用的skills。
-   如果感觉上下文受限，请求用户提供清理指导。

## 错误恢复

当卡住或遇到错误时：
1.  重新阅读当前任务定义。
2.  检查是否已加载所需skills。
3.  在 `ai-development-guide` skills中查找反模式。
4.  如果无法解决，请求用户澄清。

## 文件组织

**任务** (`.agents/tasks/`):
- `task-analysis.md`: **入口点**
- `work-planning.md`: 创建工作计划
- `technical-design.md`: 设计文档
- `acceptance-test-generation.md`: 测试骨架生成
- `implementation.md`: 实现指南
- `quality-assurance.md`: 质量标准

**工作流** (`.agents/workflows/`):
- `agentic-coding.md`: 中/大规模工作流

**上下文地图** (`.agents/context-maps/`):
- `task-skills-matrix-cn.yaml`: 任务到skills的映射

## 需要避免的反模式

1. **跳过 `task-analysis.md`** → **始终** 从任务分析开始。
2. **预先加载所有skills** → 根据任务需求逐步加载。
3. **忽略任务的进入/退出条件** → 在每一步验证关卡。
4. **没有任务定义就工作** → 任务定义了要构建的“内容”。
5. **假设总是需要工作流** → 小任务可以直接使用任务定义。
6. **过早选择工作流** → 让任务分析来决定方法。

## 成功指标

内部跟踪：
- 任务完成率。
- 实际使用的skills与加载的skills对比。
- 质量检查通过率（应为100%）。
- 合适的路径选择（直接执行 vs 工作流）。
